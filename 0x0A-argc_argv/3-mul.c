#include "main.h"

/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
int a;
int b;
if (argc == 3)
{
printf("%d\n", a * b);
return (0);
}

printf("Error\n");
return (1);
}
